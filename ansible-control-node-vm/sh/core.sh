#!/bin/bash

# curl
echo "--------------------------------------------------------------------------------------"
echo "Installing curl..."
sudo apt update
sudo apt install -y curl
sudo apt install -y sshpass
